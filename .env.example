# NVIDIA NIM API Key — get yours free at https://build.nvidia.com
# Sign up, pick any free endpoint model, copy your API key
VITE_NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Backend DB (SQLite by default — no setup needed)
DATABASE_URL=./learnit.db

# Server port
PORT=3001
